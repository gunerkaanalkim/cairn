/** Default number of rows shown per page. */
export const DEFAULT_PAGE_SIZE = 10;

/** Default sort cycle applied when a header is activated. */
export const DEFAULT_SORT_CYCLE = ['asc', 'desc', null] as const;

/** Placeholder text rendered when no rows survive the filters. */
export const DEFAULT_EMPTY_MESSAGE = 'No records found';